language core;

extend with #exceptions, #exception-type-declaration;

exception type = Nat

fn foo(x : Bool) -> Nat {
  return if x then 0 else throw(1)
}

fn main(n : Nat) -> Nat {
  return foo(true)
}
