# typechecker

### Building
typechecker is written in Haskell, so, like most Rust Haskell, it uses stack as its build system. To compile typechecker, run:
```
stack build
```

### Usage
typechecker can be used with stack as
```
stack run
```
Also, after stack build, the compiled binary file is located in the .stack-work/install folder.